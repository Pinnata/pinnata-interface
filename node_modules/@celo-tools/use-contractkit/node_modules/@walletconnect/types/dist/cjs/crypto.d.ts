export declare namespace CryptoTypes {
    interface Participant {
        publicKey: string;
    }
    type Self = KeyPair;
    interface Peer<M = any> extends Participant {
        metadata?: M;
    }
    interface KeyPair {
        privateKey: string;
        publicKey: string;
    }
    interface EncryptKeys {
        sharedKey: string;
        publicKey: string;
        iv?: string;
    }
    interface EncryptParams extends EncryptKeys {
        message: string;
    }
    interface DecryptKeys {
        sharedKey: string;
    }
    interface DecryptParams extends DecryptKeys {
        encrypted: string;
    }
}
//# sourceMappingURL=crypto.d.ts.map