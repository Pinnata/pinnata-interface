import { Connector, Network } from '../types';
/**
 * Connector-related configuration.
 */
export interface UseConnectorConfig {
    /**
     * The address connected.
     */
    address: string | null;
    network: Network;
    updateNetwork: (network: Network) => void;
    connector: Connector;
    connect: () => Promise<Connector>;
    destroy: () => Promise<void>;
    connectionCallback: ((x: Connector | false) => void) | null;
}
export declare const useConnectorConfig: ({ networks, }: {
    networks: readonly Network[];
}) => UseConnectorConfig;
//# sourceMappingURL=useConnectorConfig.d.ts.map